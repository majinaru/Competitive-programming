#include<bits/stdc++.h>
using namespace std;
#define endl '\n';
using ll = long long;

void solve(){
    vector<int> p(5);
    iota(p.begin(), p.end(), 1);
    for(auto v : p){
        cout << v << " ";
    }
    cout << endl;
}

int32_t main(){
    cin.tie(0)->sync_with_stdio(0);
    solve();

}
